# Cloudflare Pages 部署配置
# 使用: npx wrangler pages deploy frontend --project-name storyshare

name = "storyshare"
compatibility_date = "2024-01-01"

# Pages 配置
[pages]
pages_build_output_dir = "frontend"

[vars]
# 不需要在这里配置环境变量
# Cloudflare Functions 会直接转发到阿里云后端
